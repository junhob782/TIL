# [Study] 파이썬(Python) 기초와 인공지능 생태계

## 80. 파이썬 (Python) 개요 및 특징 
* **장점**:
    1. **높은 생산성**: 문법이 영어 문장처럼 매우 간단하고 직관적이어서 배우기 쉽고 개발 속도가 빠름.
    2. **방대한 생태계**: 웹 서버(Django, FastAPI), 해킹 도구, IoT, 데이터 분석, 인공지능(AI) 등 거의 모든 분야에 활용할 수 있는 풍부한 라이브러리를 갖춤.
* **단점**:
    1. **환경 파편화**: 윈도우, 맥, 리눅스 등 운영체제마다 환경 설정 및 경로 사용 방법이 조금씩 달라 초기 세팅에 주의가 필요함.
    2. **느린 실행 속도**: C/C++ 같은 컴파일 언어에 비해 연산 속도가 느림.
* **구동 원리 (왜 느린가?)**: 
    * 파이썬은 작성된 텍스트 코드(`.py`)를 파이썬 가상 머신(PVM) 위에서 한 줄씩 실시간으로 번역(Interpret)하며 실행함. 이 번역기(인터프리터, 주로 CPython)가 C언어로 작성되어 동작하는 과정에서 오버헤드가 발생하기 때문에 근본적으로 느림.

---

## 81. Python vs PyTorch vs PyCharm 완벽 비교
> *파이썬 생태계의 3가지 핵심 요소를 명확히 구분해야 합니다.*

| 구분 | 정의 | 역할 및 특징 |
| :--- | :--- | :--- |
| **Python** | **프로그래밍 언어 (Language)** | 컴퓨터에게 명령을 내리기 위한 기본 문법이자 규칙. (예: `if`, `for`, `print`). 모든 AI와 웹 개발의 뼈대가 되는 범용 언어. |
| **PyTorch** | **머신러닝 전용 프레임워크 (Framework)** | 파이썬 언어로 만들어진(내부 연산은 C/C++로 최적화된) **인공지능 도구 상자**. 메타(Meta)에서 개발.<br>- *역할*: 신경망 공식(예: $Y = W \cdot X + b$)이나 역전파, GPU 연산 등 복잡한 수학/AI 코드를 파이썬 위에서 쉽게 조립할 수 있게 해주는 특수 목적 부품. |
| **PyCharm** | **통합 개발 환경 (IDE)** | 파이썬 코드를 쉽고 편하게 작성할 수 있도록 도와주는 **전용 텍스트 에디터(프로그램)**. JetBrains에서 개발.<br>- *역할*: 자동 완성, 코드 오류 하이라이팅, 디버깅 기능 등을 제공하여 개발자의 생산성을 극대화해 줌. |

---

## 82. 파이썬 코드의 기본 구성 요소
1. **표현식 (Expression)**: 파이썬에서 어떠한 '값'을 만들어내는 간단한 코드. (예: `273`, `10 + 20`, `"Hello"`)
2. **문장 (Statement)**: 표현식이 하나 이상 모여서 **실행 가능한 하나의 동작**을 구성한 것. (그 자체로 어떠한 결과를 만들어내지 못하면 문장이 아님).
3. **프로그램 (Program)**: 여러 개의 문장이 모여 논리적인 흐름을 형성한 것.
4. **리터럴 (Literal)**: 변수에 할당되는, 코드 상에 직접 입력된 '어떠한 값 자체' (자료).
5. **연산자 (Operator)**: 스스로 값이 되는 것이 아니라, 값(리터럴)들 사이에 들어가 계산 등 무언가 연산을 지시하는 기호. (예: `+`, `-`, `*`)
6. **주석 (Comment)**: 프로그램의 실제 실행 진행에는 전혀 영향을 주지 않는 코드. 주로 개발자가 프로그램 코드에 대한 설명이나 메모를 남기기 위해 사용함 (`#` 기호 사용).

---

## 83. 식별자와 네이밍 컨벤션 (Naming Convention)
* **키워드 (Keyword)**: 파이썬에서 특별한 의미로 사용하기로 미리 예약해 놓은 단어 (예: `True`, `False`, `for`, `if`). 식별자로 사용할 수 없음.
* **식별자 (Identifier)**: 프로그래밍 언어에서 변수나 함수, 클래스 등에 **이름을 붙일 때 사용하는 단어**.
    * **규칙**: 키워드 사용 불가, 숫자로 시작 불가, 공백 포함 불가, 특수문자는 언더바(`_`)만 허용됨. 의미가 명확한 알파벳 단어로 짓는 것이 관례임.

### 83.1 대표적인 표기법 (Naming Case)
* **스네이크 케이스 (Snake Case)**: 단어들 사이에 언더바(`_`)를 붙여서 뱀처럼 길게 이어 쓰는 방식. 주로 **변수명, 함수명**에 사용함. (예: `login_user_name`)
* **캐멀 케이스 (Camel Case / Pascal Case)**: 단어들의 첫 글자를 대문자로 만들고 언더바 없이 이어 쓰는 방식. 낙타의 혹처럼 보임. 파이썬에서는 주로 **클래스명**에 사용함. (예: `LoginUserName`)

---

## 84. 함수(Function)와 메서드(Method), 생성자(Constructor)
* **함수 (Function)**: 특정 객체나 클래스와 상관없이 전역적으로 혼자 독립해서 존재할 수 있는 기능.
* **메서드 (Method)**: 특정 객체(클래스) 안에 종속되어 들어있는 함수. 
    * > **아키텍트 노트**: 자바의 `this`와 달리, 파이썬에서 클래스 내부의 메서드는 첫 번째 매개변수로 반드시 **`self`**를 가져야 합니다. `self`는 생성자가 아니라 '자기 자신 객체'를 가리키는 참조자입니다.
* **생성자 (Constructor)**: 클래스(설계도)를 바탕으로 객체가 힙 메모리에 올라갈 때, 제일 먼저 자동으로 호출되어 변수들을 초기화해 주는 특별한 메서드 (파이썬에서는 `__init__` 메서드 사용). 직접 만들지 않으면 아무 일도 하지 않는 '디폴트 생성자'가 자동으로 동작함.
# [Study] 파이썬 객체지향 흐름과 기본 자료형

## 85. 파이썬의 객체지향 프로그래밍 (OOP) 흐름
* 파이썬은 내부의 거의 모든 데이터가 객체(Object)로 이루어진 언어입니다.
* **동작 흐름**: `프로그램` $\rightarrow$ `O.O.P (객체지향 프로그래밍)` $\rightarrow$ Base: `클래스(Class)` $\rightarrow$ `인스턴스(Instance)` 생성 $\rightarrow$ `캡슐화 / 상속 / 다형성` 특징 활용.

---

## 86. 자료(Data)와 출력 함수
* **자료 (Data)**: 프로그램이 메모리에 올려서 연산하고 처리할 수 있는 모든 것을 의미함.
* **`print()` 함수**: 파이썬의 표준 출력 함수. 출력하고 싶은 자료나 변수들을 괄호 `()` 안에 나열하여 콘솔 화면에 보여줌.

---

## 87. 파이썬의 3대 기본 자료형 (Data Types)
> *파이썬이 메모리에 데이터를 저장하고 취급하는 기본 형태*

| 자료형 | 영문명 및 클래스 | 의미 및 활용 예시 |
| :--- | :--- | :--- |
| **문자열** | String (`str`) | 메시지 내용, 메일 제목 등 글자들의 집합. (예: `"안녕하세요"`) |
| **숫자** | Number (`int`, `float`) | 물건의 가격, 성적, 센서값 등 계산이 가능한 수치. (예: `273`, `3.14`) |
| **불** | Boolean (`bool`) | 참(`True`)과 거짓(`False`) 두 가지 값만 가짐. 친구의 로그인 상태, 스위치 On/Off 등 상태 판별에 사용. |

### 87.1 자료형 확인 함수: `type()`
* 현재 다루고 있는 값이나 변수가 어떤 자료형(클래스)인지 확인하고 싶을 때 사용하는 내장 함수.
* **사용 예시**: `print(type("안녕하세요"))` $\rightarrow$ 실행 결과로 `<class 'str'>`이 출력됨.

---

## 88. [핵심] 문자열의 불변성 (Immutability)
* **특징**: 파이썬에서 한 번 메모리에 생성된 문자열은 **임의로 부분 수정이 절대 불가능**함.
* **해결책**: 원본 문자열의 일부를 수정하는 대신, 가공을 거쳐 **새로운 문자열 객체를 만들어** 새로 할당(작성)해야 함.
* > **아키텍트 노트**: C언어의 문자 배열처럼 `text[0] = 'A'` 방식으로 글자 하나만 쏙 바꾸려고 시도하면 파이썬은 즉시 에러(`TypeError`)를 발생시킵니다. 메모리 안정성을 위한 파이썬의 매우 중요한 특징입니다.

---

## 89. 독스트링 (DocString)과 멀티라인 문자열
* **형태**: 큰따옴표 3개 `"""` 또는 작은따옴표 3개 `'''`로 문자열의 위아래를 감싸서 표현함.
* **용도 및 특징**:
    1. **멀티라인 출력**: 코드 내부에 엔터(줄바꿈)를 쳐서 작성한 형태(WYSIWYG) 그대로 화면에 똑같이 출력해 줌. (별도의 `\n` 제어 문자가 필요 없음).
    2. **서류형 문자열 (DocString)**: 클래스나 함수가 시작되는 바로 밑에 작성하여, 해당 코드의 목적, 파라미터, 반환값 등을 설명하는 **공식 설명서(메뉴얼)** 역할을 함.

# [Study] 파이썬 문자열 인덱싱/슬라이싱과 기본 연산

## 90. 문자열 (String) 개요와 인덱스 (Index)
* **문자열 (String)**: 하나 또는 여러 개의 문자로 구성된 기본 데이터 형.
* **인덱스 (Index)**: 데이터 구조 내에서 요소의 '위치'를 나타내는 번호표.
    1. **제로 인덱스 (Zero Index)**: 숫자를 **0**부터 셈. *(파이썬, C, 자바 등 대부분의 언어가 채택)*
    2. **원 인덱스 (One Index)**: 숫자를 **1**부터 셈. *(R, MATLAB 등 일부 통계/수학 언어에서 사용)*
* > **아키텍트 팁 (음수 인덱싱)**: 파이썬은 뒤에서부터 요소를 찾을 때 사용하는 **음수 인덱스**를 지원합니다. 맨 마지막 문자는 `-1`, 그 앞은 `-2`가 됩니다. (예: `text[-1]`)

---

## 91. 인덱싱(Indexing)과 슬라이싱(Slicing) 
> *문자열 내부의 데이터를 추출하는 파이썬의 가장 강력하고 편리한 무기*

### 91.1 인덱싱 (문자 선택 연산자: `[]`)
* **개념**: `[]` 기호를 이용해 문자열의 **특정 위치(인덱스)에 있는 문자 딱 1개**를 콕 집어 참조하는(가져오는) 것.
* **사용법**: 대괄호 안에 선택할 문자의 위치를 지정함. (예: `name[0]`)

### 91.2 슬라이싱 (부분 추출 연산자: `[:]`)
* **개념**: `[:]` 기호를 이용해 문자열의 **특정 구간(일부)을 통째로 잘라내어 추출**하는 것. 
* **사용법**: `문자열[시작위치:끝위치]` 형태로 사용함. (단, '끝 위치'의 문자는 포함되지 않고 그 앞까지만 잘림).
* **[핵심 특징] 원본 보존**: 슬라이싱을 통해 잘라낸 문자열은 완전히 새로운 문자열 객체로 반환되며, **원본 문자열 데이터는 절대 변하지 않음**. (문자열의 불변성).

---

## 92. 내장 함수와 예외 처리 (len & IndexError)

### 92.1 문자열 길이 확인: `len()` 함수
* **역할**: Length의 약자로, 문자열의 전체 길이를 구할 때 활용하는 파이썬 내장 함수.
* **사용법**: 괄호 내부에 문자열(또는 변수)을 넣으면 문자열을 구성하는 총 문자 개수를 정수로 반환해 줌. (예: `len("Hello")` $\rightarrow$ `5`)

### 92.2 `IndexError` 예외 (런타임 오류)
* **발생 원인**: 인덱싱을 할 때, 실제 문자열이나 리스트가 **가진 길이를 넘어서는(존재하지 않는) 요소나 글자의 인덱스 번호를 선택**할 경우 발생하는 에러.
* **방어 코딩**: 인덱스 번호를 변수로 사용할 때는 항상 `len()` 함수를 이용해 문자열의 실제 길이를 초과하지 않도록 검사하거나 제어해야 함.

---

## 93. 복소수 (Complex Number) 자료형
* **사용 이유**: 수학 연산, 특히 인공지능(AI), 신호 처리, 양자 컴퓨팅 등에서 **계산을 편리하고 직관적으로 하기 위해** 사용함.
* **파이썬의 특징**: 파이썬은 내부적으로 복소수를 기본 자료형으로 지원함. (단, 수학에서 허수부를 표현하는 `i` 대신, 파이썬 등 공학 분야에서는 전류 `i`와 헷갈리지 않도록 **`j`**를 사용함. 예: `3 + 4j`).
# [Study] 파이썬 숫자형 연산과 변수, 그리고 C++ 메모리 심화

## 94. 파이썬의 숫자형 자료형 (Numeric Types)
* **정수형 (Integer, `int`)**: 소수점이 없는 숫자. 
    * 예시: `0`, `1`, `273`, `-52`
* **실수형 (Floating Point, `float`)**: 소수점이 있는 숫자. 
    * 예시: `0.0`, `52.273`, `-1.2`

---

## 95. 파이썬 사칙연산과 특수 연산자
| 연산자 | 기능 | 설명 및 특징 |
| :---: | :--- | :--- |
| **`+`** | 덧셈 | 숫자 간의 덧셈 (문자열 간에는 연결 연산자로 작동) |
| **`-`** | 뺄셈 | 숫자 간의 뺄셈 |
| **`*`** | 곱셈 | 숫자 간의 곱셈 |
| **`/`** | 나눗셈 | 기본 나눗셈 (파이썬3에서는 나누어 떨어져도 항상 **실수형**으로 결과가 나옴) |
| **`%`** | 나머지 | 나눗셈 후 남은 나머지 값을 구함 (짝수/홀수, 배수 판별에 유용) |
| **`//`** | **정수 나누기** | 숫자를 나눈 후 소수점 이하 자리를 버리고 **정수 부분의 몫만 남김** |
| **`**`** | **제곱** | 왼쪽 숫자를 오른쪽 숫자만큼 거듭제곱함 (예: `2 ** 3` $\rightarrow$ `8`) |

---

## 96. 변수 (Variable)와 복합 대입 연산자
* **변수**: 데이터를 저장할 때 사용하는 식별자(이름표).
* **변수의 활용 3단계**:
    1. **선언 및 생성**: 사용할 변수의 이름을 정함 (파이썬은 값을 할당하는 순간 자동으로 선언됨).
    2. **할당**: 변수에 실제 값을 넣음 (대입 연산자 `=` 사용).
    3. **참조**: 변수 이름을 호출하여 안에 들어있는 값을 꺼내어 사용함.

### 96.1 복합 대입 연산자
> *변수 자신의 값에 특정 연산을 수행한 후, 그 결과를 다시 자신에게 대입하는 축약형 연산자*
* `+=` : 숫자를 덧셈 후 대입 (예: `a += 1` $\rightarrow$ `a = a + 1`)
* `-=` : 숫자를 뺄셈 후 대입
* `*=` : 숫자를 곱셈 후 대입
* `/=` : 숫자를 나눗셈 후 대입
* `%=` : 숫자의 나머지를 구한 후 대입
* `**=` : 숫자를 제곱 후 대입

---

## 97. 사용자 입력과 블로킹 (Blocking)
* **`input()` 함수**: 명령 프롬프트(콘솔)에서 사용자로부터 데이터를 키보드로 입력받을 때 사용하는 파이썬 내장 함수. 
    * *비교*: C언어의 `scanf()` 기능과 동일함.
    * **[주의]**: `input()`을 통해 입력받은 모든 데이터는 숫자를 입력하더라도 **무조건 문자열(String) 자료형**으로 저장됨. 숫자로 계산하려면 `int()`나 `float()`로 형변환(Casting)을 해야 함.
* **블록 (Block / Blocking)**: `input()` 함수가 호출되면, 사용자가 데이터를 입력하고 Enter 키를 누를 때까지 **프로그램의 실행 흐름이 잠시 멈추고 대기하는 상태**를 의미함.

---

## 98. [C++ 심화] 프리 스토어(Free Store) vs 힙(Heap) 
> *파이썬과 달리 메모리를 직접 제어해야 하는 C/C++ 환경에서의 동적 메모리 구조 분석*

두 용어는 실무에서 혼용되지만, C++ 표준에 따르면 엄밀한 관리 체계의 차이가 존재합니다.

### 98.1 핵심 차이점 비교
| 구분 | 할당/해제 연산자 | 특징 |
| :--- | :--- | :--- |
| **프리 스토어 (Free Store)** | C++의 **`new`** / **`delete`** | 객체 지향적. 메모리 할당과 동시에 **생성자(Constructor)/소멸자**를 자동 호출함. |
| **힙 (Heap)** | C의 **`malloc()`** / **`free()`** | 절차 지향적. 단순히 요청한 크기만큼의 메모리 공간만 할당함 (생성자 호출 안 함). |
*(※ 대부분의 컴파일러에서 이 두 영역은 물리적으로 같은 메모리 공간을 공유하지만, 개념적으로는 완전히 다른 관리 체계를 가짐).*

### 98.2 프리 스토어의 주요 특징
1. **동적 할당 (Dynamic Allocation)**: 컴파일 타임이 아닌, 프로그램 실행 중(Runtime)에 필요한 만큼의 메모리를 운영체제에 요청함.
2. **수동 관리**: 프로그래머가 직접 메모리를 해제해야 함. (`delete`를 누락하면 **메모리 누수(Memory Leak)**가 발생하여 시스템이 뻗을 수 있음).
3. **유연한 수명**: 함수 블록이 종료되면 자동으로 사라지는 스택(Stack) 영역의 지역 변수와 달리, 개발자가 원할 때까지 데이터를 계속 유지할 수 있음.

# [Study] 파이썬 타입 캐스팅과 문자열 데이터 가공

## 99. 형변환 (Type Casting)
> *데이터의 자료형(Type)을 다른 자료형으로 강제로 바꾸는 작업*

* **캐스팅의 필요성**: `input()` 함수의 입력 자료형은 항상 문자열(String)이므로, 입력받은 데이터를 숫자 연산에 활용하기 위해서는 반드시 숫자로 변환하는 과정이 필요함.
* **주요 형변환 함수**:
    * `int()`: 문자열(또는 실수)을 정수(int) 자료형으로 변환.
    * `float()`: 문자열(또는 정수)을 실수(float) 자료형으로 변환.
    * `str()`: 숫자 등의 다른 자료형을 문자열(str)로 변환.

---

## 100. 변수 제어 및 입력 (Review)
* **변수 선언**: 변수를 생성하는 것.
* **변수 할당**: 생성된 변수에 실제 데이터를 넣는 것 (`=` 기호 사용).
* **변수 참조**: 변수에 저장된 값을 꺼내어 사용하는 것.
* **`input()`**: 명령 프롬프트(콘솔)에서 사용자로부터 데이터를 입력받아 문자열로 반환하는 함수.
* **`bool` (불리언)**: 논리 연산의 결과로 오직 `True`(참) 또는 `False`(거짓) 두 가지 상태만 가지는 자료형.

---

## 101. 문자열 포매팅 (String Formatting)
* **`format()` 함수**: 문자열 중간에 변수나 데이터를 깔끔하게 끼워 넣을 때 사용하는 템플릿 함수.
* **사용 규칙**: 
    1. 뼈대가 되는 문자열 내부에 빈 공간을 뜻하는 중괄호 `{}`를 배치함.
    2. 문자열 뒤에 마침표(`.`)를 찍고 `format()` 함수를 호출함.
    3. **[주의]** 중괄호 `{}`의 개수와 `format()` 함수 안 매개변수(넣을 값)의 개수는 반드시 같아야 함.
* **동작 원리**: 문자열의 `{}` 기호가 `format()` 함수 괄호 안의 매개변수로 차례대로 대치(Mapping)되면서, 숫자든 무엇이든 최종적으로 완벽한 하나의 문자열로 완성됨.

---

## 102. 필수 문자열 가공 메서드 (String Methods) 

| 메서드명 | 기능 설명 | 실무 활용 예시 |
| :--- | :--- | :--- |
| **`upper()`** | 문자의 알파벳을 모두 **대문자**로 변환함. | 대소문자 구분 없이 검색어 비교할 때 |
| **`lower()`** | 문자의 알파벳을 모두 **소문자**로 변환함. | 이메일 주소 표준화 저장 시 |
| **`strip()`** | 문자열 **양옆의 모든 공백**(띄어쓰기, 줄바꿈 등)을 제거함. | 사용자 입력값의 불필요한 여백 처리 시 |
| **`lstrip()`** | 문자열 **왼쪽(Left)**의 공백만 제거함. | |
| **`rstrip()`** | 문자열 **오른쪽(Right)**의 공백만 제거함. | |

---

## 103. 문자열 탐색 및 분할

### 103.1 문자열 위치 찾기
* **`find()`**: 문자열의 **왼쪽부터** 탐색하여, 지정한 문자열이 처음 등장하는 인덱스(위치 번호)를 반환함.
* **`rfind()`**: 문자열의 **오른쪽부터(Right)** 탐색하여, 처음 등장하는 위치를 반환함.
* > **아키텍트 팁 (에러 방지)**: 만약 찾는 문자열이 내부에 존재하지 않으면, 이 두 함수는 프로그램 에러를 발생시키지 않고 안전하게 **`-1`을 반환**합니다.

### 103.2 내부 존재 여부 확인 (`in` 연산자)
* **개념**: 문자열(또는 리스트 등) 내부에 특정 문자열이 포함되어 있는지 직관적으로 확인할 때 사용하는 연산자.
* **결과**: 존재하면 `True`, 없으면 `False` (bool 형태)로 출력됨.
    * *예시*: `"a" in "apple"` $\rightarrow$ `True`

### 103.3 문자열 자르기 (`split`)
* **`split()` 함수**: 하나의 긴 문자열을 특정한 문자(구분자)를 기준으로 여러 조각으로 자름.
* **동작 결과**: 쪼개진 문자열 조각들은 대괄호 `[]`로 묶인 **리스트(List) 자료형**으로 변환되어 반환됨. (구분자를 지정하지 않으면 띄어쓰기를 기준으로 자름).

# [Study] 파이썬 자료구조의 본질과 시스템 아키텍처 심화

## 106. 파이썬 기초 구문 특징
* **들여쓰기(Indent) 민감성**: 파이썬은 중괄호 `{}` 대신 들여쓰기(공백 4칸 권장)로 코드의 블록(범위)을 구분하므로 줄 맞춤에 매우 엄격함.
* **False로 변환되는 값 (Falsy)**: 조건문에서 거짓(`False`)으로 취급되는 값들.
    * `None`, `0`, 빈 문자열(`""`), 빈 바이트열(`b""`), 빈 리스트(`[]`), 빈 딕셔너리(`{}`) 등 데이터가 '비어있음'을 뜻하는 값들.
* **`pass` 키워드**: 전체적인 골격을 먼저 잡아두고, 내부 로직은 "나중에 구현하겠다"고 남겨둘 때 사용하는 더미(Placeholder) 키워드. (에러 발생 방지).

---

## 107. 리스트(List)와 배열(Array)의 본질적 차이 
| 구분 | 특징 | 내부 메모리 구조 |
| :--- | :--- | :--- |
| **리스트 (Python List)** | 숫자, 문자열 등 **여러 자료형을 섞어서 저장할 수 있음**. (`[]` 사용) | **포인터 배열 (Array of Pointers)**: 실제 데이터가 아닌, 데이터가 저장된 메모리 주소(포인터)들을 모아둔 배열이므로 다양한 크기와 타입 수용 가능. |
| **배열 (C언어 Array)** | 선언 시 지정한 **같은 자료형만** 넣을 수 있음. | 연속된 메모리 공간에 실제 데이터 값 자체가 일렬로 빽빽하게 저장됨. |

* **`extend()` 메서드**: 원래 있던 리스트의 맨 뒤에, 새로운 리스트의 요소들을 쫙 풀어서 연장(추가)하는 함수.
* *(참고: C언어의 배열은 시작 주소부터 연속적으로 읽어내기 최적화되어 있어, `for`문과 영혼의 단짝(세트)처럼 쓰입니다.)*

---

## 108. 튜플(Tuple) - 대표적인 비파괴적 자료형
* **개념**: 리스트와 비슷하게 여러 자료를 모아두지만, **한 번 생성되면 내부 요소를 절대 추가/삭제/수정할 수 없는(비파괴적, Immutable) 자료형**. `()`를 사용하여 선언함.
* **장점**: 데이터가 변하지 않음을 보장하므로 메모리를 적게 차지하고 처리 속도가 빠름.

---

## 109. 딕셔너리(Dictionary)
* **개념**: 데이터를 순서(인덱스)가 아닌 **키(Key)**를 기반으로 저장하고 식별하는 자료형. `{}` 사용.
* **구조**:
    * **키 (Key)**: 딕셔너리 내부에서 값에 접근(검색)할 때 사용하는 고유한 이름표.
    * **값 (Value)**: 딕셔너리 내부에 있는 실제 데이터 내용.
* **키(Key) 충돌 시 동작**: 동일한 키 이름으로 새로운 값을 할당하면, 프로그램 오류(에러)가 나지 않고 **가장 최근에 입력된 값으로 덮어씌워짐(Overwrite)**.

---

## 110. 데이터 직렬화 포맷: JSON vs XML 
* **XML (eXtensible Markup Language)**: `<name>홍길동</name>` 처럼 시작 태그와 종료 태그가 항상 짝을 이뤄야 함. 구조는 명확하지만 문자열 길이가 길어져 무거움.
* **JSON (JavaScript Object Notation)**: `{"name": "홍길동"}` 처럼 파이썬 딕셔너리와 똑같은 구조를 가짐. 태그를 닫아줄 필요가 없어 텍스트 용량이 가벼움 $\rightarrow$ **네트워크 트래픽(데이터)을 아끼기 위해 현대 서버 통신의 표준으로 자리 잡음**.

---

## 111. 프로그램 오류(Error)와 예외(Exception)
* **오류 (Error / Syntax Error)**: 괄호 누락, 오타 등으로 인해 코드 구문이 잘못되어, **프로그램을 실행하기도 전(컴파일/파싱 단계)에 죽는 것이 확정**된 치명적 문제.
* **예외 (Exception / Runtime Error)**: 코드는 문법적으로 완벽해서 실행은 되지만, **실행 해봐야만 알 수 있는 문제** (예: 파일 없음, 0으로 나누기, 네트워크 끊김 등).

---

## 112. [아키텍트 심화] 예외 처리의 시스템적 위험성
1. **예외의 불확실성과 CPU 분기 예측 방해**: `try-catch` 같은 예외 처리가 발동하면, 순차적이던 프로그램 흐름이 강제로 '점프'해서 날아감. 이는 반도체(CPU)의 파이프라인과 분기 예측(Branch Prediction)을 깨뜨려, 다음에 어떤 코드가 실행될지, 시간이 얼마나 걸릴지 예측하기 어렵게 만듦.
2. **교착 상태(Deadlock) 및 시스템 프리징(Freeze) 위험**: 특정 하드웨어(모터, 센서 등)를 제어하고 결과를 기다리던 함수가 `return`으로 정상 종료되지 못하고 예외로 튕겨버리면, **하드웨어 제어권(Lock)이나 반환 신호를 영원히 돌려주지 못함**. 이로 인해 하드웨어는 "작업 끝났으니 가져가세요!"라며 무한 대기 상태에 빠지고, 시스템 전체가 굳어버리는 치명적 결함이 발생할 수 있음.

---

## 113. 프로그래밍 언어별 메모리 관리 체계 
> *다 쓴 메모리(쓰레기)를 누가 치우는가?*

| 관리 주체 | 대상 프로그래밍 언어 | 특징 |
| :--- | :--- | :--- |
| **자동 관리 (Managed)** | `Python`, `Java`, `JavaScript`, **`C#`** | **가비지 컬렉터(GC)**라는 백그라운드 청소부가 주기적으로 안 쓰는 메모리를 알아서 해제해 줌. 개발이 편하지만 메모리 청소 시점에 약간의 렉(멈춤)이 발생할 수 있음. |
| **수동 관리 (Unmanaged)** | `C`, `C++`, `Rust` | 개발자가 직접 `free()`, `delete`를 통해 해제해야 함. (단, Rust는 컴파일러가 소유권 규칙으로 해제 코드를 자동 삽입함). 제어가 정밀하고 빠르지만, 실수하면 메모리 누수가 발생함. |
