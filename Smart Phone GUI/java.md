# [Study] Java 백엔드(AOP)와 로보틱스 산업 동향

## 55. Java 기초와 AOP (관점 지향 프로그래밍)
* **객체지향 프로그래밍 (OOP)**: 상태와 행위를 가진 '객체' 단위로 나누어 개발하는 Java의 기본 철학. 하지만 프로젝트가 커지면 중복 코드가 발생함.
* **관점 지향 프로그래밍 (AOP - Aspect-Oriented Programming)**: OOP를 보완하는 기법. 핵심 비즈니스 로직과 시스템 전반에 걸친 공통 부가 기능을 분리하여 관리함.
    * **횡단 관심사 (Cross-cutting Concerns)**: 시스템 전반에 걸쳐 흩어져 있는 공통 기능 (예: 메서드 호출 시간 로깅, 데이터 암호화/복호화, 트랜잭션 관리 등).
    * **애스펙트 (Aspect)**: 분리해낸 횡단 관심사들을 모듈화한 단위 (공통 기능들의 묶음).

---

## 56. Spring Framework 생태계와 백엔드 설계 
* **위빙 (Weaving)**: Spring 프레임워크(Spring.io)가 제공하는 핵심 기능 중 하나. 런타임이나 컴파일 타임에 `Aspect`(부가 기능)를 원본 핵심 코드에 **끼워 넣는(삽입하는) 과정**을 의미함. (원본 코드를 수정하지 않고도 부가 기능을 적용할 수 있게 해줌).
* **디자인 패턴 (Design Patterns)**: 소프트웨어 설계 시 자주 발생하는 문제들을 해결하기 위한 모범 답안(재사용 가능한 객체지향 설계 템플릿). Spring 내부도 팩토리(Factory), 싱글톤(Singleton), 프록시(Proxy) 패턴 등으로 이루어져 있음.
* **SQL과 데이터 모델링 (Modeling)**: 비즈니스 요구사항에 맞춰 데이터베이스의 테이블 구조와 관계(ERD)를 설계하는 과정.

---

## 57. 로봇 (ROBOT) 산업 분류 및 주요 기업 동향
> *소프트웨어(AI/제어)와 하드웨어(기구부)가 결합하는 최전선*

### 1. 휴머노이드 로봇 (Humanoid)
* **특징**: 인간과 유사한 형태를 가진 미래 지향적 로봇. 이족 보행 및 복잡한 균형 제어 기술이 필요함.
* **주요 기업 및 기술**:
    * **Boston Dynamics (보스턴 다이내믹스)**: 아틀라스(Atlas) 등 세계 최고 수준의 동적 제어 기술 보유.
    * **레인보우 로보틱스**: 카이스트 휴보(HUBO) 연구진이 창업한 국내 대표 휴머노이드 기업.
    * **로보티스 (ROBOTIS)**: 로봇 전용 액추에이터(다이나믹셀) 및 자율주행 로봇 제조.
    * > *아키텍트 노트: 최신 로봇 제어는 **ROS2 (Robot Operating System 2)**를 기반으로 합니다. 이는 스마트폰의 **Android OS**처럼, 다양한 제조사의 모터와 센서 등 하드웨어를 추상화하여 개발자가 소프트웨어(앱) 단위로 로봇을 제어할 수 있게 해주는 혁신적인 플랫폼입니다.*

### 2. 산업용 로봇 (Industrial Robot)
* **특징**: 공장 조립 라인 등에서 사용되며 거대하고, 무겁고, 비쌈. 작업자의 안전을 위해 반드시 펜스(Fence)를 치고 격리된 상태에서 매우 빠른 속도로 반복 작업을 수행함.
* **주요 기업**: 현대 로보틱스 (국내 산업용 로봇 점유율 1위) 등.

### 3. 협동 로봇 (Cobot - Collaborative Robot)
* **특징**: 휴머노이드와 기존 산업용 로봇의 중간 형태. 인간과 **같은 작업 공간에서 펜스 없이 상호작용(협동)**하며 일할 수 있도록 안전하게 설계된 신기술.
* **핵심 요구 기술**: 충돌을 감지하고 멈추는 기계전자(Mechatronics) 기술, AI 시각 처리, 그리고 모터를 정밀하게 제어하는 **펌웨어(Firmware)** 프로그래밍 역량이 필수적임.

## 58. 자바 가상 머신 (JVM: Java Virtual Machine) 
* **개요**: 자바 프로그램이 운영체제(OS)나 하드웨어에 종속되지 않고 실행될 수 있도록 도와주는 중간 실행 엔진.
* **스마트폰 플랫폼 (Android)**: 안드로이드 역시 다양한 제조사의 스마트폰 하드웨어 파편화를 극복하기 위해 가상 머신 기술(과거 Dalvik, 현재 ART)을 채택하여 앱(App)을 구동함.

### 58.1 가상 머신(VM) 도입의 장단점
* **장점 (WORA - Write Once, Run Anywhere)**: 하드웨어나 운영체제를 가리지 않고, 한 번 작성한 코드를 어디서나 동일하게 실행할 수 있음.
* **단점**:
    1. C/C++ 처럼 하드웨어에 맞춰 직접 빌드된 **최적화된 바이너리에 비해 실행 속도가 다소 느릴 수 있음** (중간 통역 과정이 필요하기 때문).
    2. 각 플랫폼(Windows, Linux, Mac 등)에 맞는 가상 머신(JRE/JDK)이 사전에 설치되어 있어야만 동작함.

---

## 59. 자바(Java)의 장단점 분석

### 59.1 자바의 강력한 장점
1. **객체지향 프로그래밍 (OOP)**: 간결하면서도 강력한 객체지향 설계(캡슐화, 상속, 다형성)를 지원하여 유지보수성이 뛰어남.
2. **개방형 구조 (Open Architecture)**: 여러 외부 기술 및 라이브러리와의 융합이 매우 용이함.
3. **플랫폼 독립성 (Platform Independent)**: 운영체제나 하드웨어 아키텍처가 바뀌어도 소스 코드를 다시 컴파일할 필요 없이 동일하게 실행됨.
4. **압도적인 생태계 (Ecosystem)**: Spring, Hibernate 등 수많은 검증된 오픈소스 프레임워크가 존재하여 개발 생산성을 극대화하고 유지보수 비용을 획기적으로 절감함.

### 59.2 자바의 한계와 단점
1. **극단적인 처리 속도 요구 분야**: 마이크로초(us) 단위의 성능이 중요한 시스템(예: OS 커널, 초고속 트레이딩 엔진)에는 적합하지 않을 수 있음. 
    * *(아키텍트 노트: 단, 일반적인 웹/백엔드 서버 환경에서는 JIT 컴파일러 덕분에 C/C++에 크게 뒤지지 않는 우수한 속도를 냅니다.)*
2. **GUI 애플리케이션 한계**: MS 윈도우 기반의 화려하고 빠릿빠릿한 데스크톱 응용 프로그램(GUI) 개발에는 C#이나 C++(MFC/Qt)에 비해 무겁고 적합하지 않음.
3. **하드웨어 직접 제어 불가**: 가상 머신(JVM)이라는 샌드박스 위에서 동작하므로, 메모리 주소(포인터)에 직접 접근하거나 하드웨어(센서, 액추에이터)를 정밀하고 민첩하게 조정해야 하는 펌웨어 프로그래밍에는 적합하지 않음.

---

## 60. 빅데이터(Big Data)와 자바의 관계 

### 60.1 빅데이터 개요
* 방대한 양의 데이터를 분석하여 의미 있는 정보(인사이트)를 추출하고, 이를 통해 사용자 맞춤형 서비스를 제공하거나 미래 현상을 예측하는 기술 (의학, 정치, 사회 등 전 분야 활용).

### 60.2 빅데이터 생태계의 표준, 자바
* 빅데이터를 수집하고 분석하는 핵심 소프트웨어(Hadoop, Apache Spark, Kafka, Elasticsearch 등)는 주로 **오픈소스 기반이며, 이들 생태계의 절대다수가 자바(Java) 또는 JVM 기반 언어(Scala 등)로 개발**되었음.
* 따라서 방대한 데이터 노드 간의 중계 및 분석 로직을 구현하고 커스터마이징하는 데 있어 자바는 대체 불가능한 필수 언어로 자리 잡고 있음.

# [Study] 객체지향 프로그래밍(OOP)과 자바 코어 기초

## 61. 프로그래밍 패러다임: 절차지향 vs 객체지향 
* **절차지향 프로그래밍 (Procedural)**:
    * **C언어 방식**: 프로시저(함수)의 호출 개념에 바탕을 둠.
    * 전체 애플리케이션을 구성하는 필요한 기능들을 순차적인 함수 호출의 결과로 연결하여 다음 작업을 진행하는 구조.
* **객체지향 프로그래밍 (OOP - Object-Oriented)**:
    * **Java 방식**: 현실 세계의 객체 모델을 바탕으로 프로그램을 구조화하고 시각화하여 개발하는 기법.
    * **독립성**: 객체는 독립적인 개체로, 필드(속성)와 메서드(행위)로 구성됨.
    * **목적**: 상속, 추상화, 캡슐화 등의 특징을 활용하여 모듈의 '재사용성'을 극대화하고, 생산성과 유지보수 효율성을 높임.

---

## 62. 클래스(Class)와 인스턴스(Instance) 
| 구분 | 정의 및 역할 | 특징 |
| :--- | :--- | :--- |
| **클래스 (Class)** | 속성이 같은 객체들을 대표하는 대상이자 **설계도(틀)**. | 필드(속성)와 메서드(행위)로 구성되며, 추상화를 통해 슈퍼 클래스(부모)와 서브 클래스(자식)로 계층을 나눌 수 있음. |
| **인스턴스 (Instance)** | 클래스라는 틀을 기반으로 메모리에 실제 생성된 **객체(Object)**. | 각각의 인스턴스는 메모리상에서 고유한 상태(데이터 값)를 가짐. |

---

## 63. 객체지향의 주요 특징 (Inheritance & Encapsulation)
* **상속 (Inheritance)**: 
    * 클래스를 추상화하거나 구체화하는 과정에서 발생. 
    * 슈퍼 클래스(부모)의 기본 구성요소를 물려받아 재사용하며, 자신만의 필드나 메서드를 추가하여 구체화(확장)할 수 있음.
* **캡슐화 (Encapsulation)**:
    * 생성한 객체가 어떤 메서드와 필드로 어떻게 일을 수행하는지 구체적인 내부 로직을 **외부에 숨기는(정보 은닉)** 특성. 안정성을 높이고 결합도를 낮춤.

---

## 64. 필드(Field)와 메서드(Method)
* **필드 (Field)**: 객체의 **속성(상태)**에 해당하며, 클래스 내부의 멤버 변수(Member Variable)라고도 함.
* **메서드 (Method)**: 객체의 **기능(행위)**에 해당하며, 클래스를 사용해 실행할 수 있는 동작을 구현함.
    * > **아키텍트 노트 (다형성 구현):** > 1. **오버로딩(Overloading)**: 같은 클래스 내에서 동일한 이름으로 파라미터(매개변수)만 다르게 여러 메서드를 정의하는 것.
      > 2. **오버라이딩(Overriding)**: 상속 관계에서 부모의 메서드를 자식 클래스에서 재정의(수정)하여 사용하는 것.

---

## 65. 접근 한정자 (Access Modifiers) 
> *클래스, 필드, 메서드의 외부 접근(노출) 범위를 제어함*

1. **`public`**: 모든 패키지, 모든 클래스에서 자유롭게 접근 가능.
2. **`protected`**: 동일 패키지에 속한 클래스, 그리고 외부 패키지더라도 상속받은 서브 클래스(자식)에서는 접근 가능.
3. **`default` (기본값)**: 한정자를 아예 쓰지 않은 상태. **오직 동일 패키지**에 속한 클래스에서만 접근 허용.
4. **`private`**: 철저히 **현재 클래스 내부**에서만 접근 가능 (캡슐화의 핵심).

---

## 66. 일반 한정자 (Non-Access Modifiers)
1. **`static`**: 객체(인스턴스)를 생성하지 않고도 접근할 수 있는 **정적(클래스 레벨)** 리소스를 선언할 때 사용. 동일 가상머신 상의 모든 객체가 메모리를 공유함.
2. **`final`**: 더 이상 변경할 수 없음을 선언하는 **종단(상수)** 한정자.
    * 변수에 사용: 값 변경 불가 (상수화).
    * 메서드에 사용: 오버라이딩(재정의) 불가.
    * 클래스에 사용: 상속(서브 클래스 생성) 불가.
3. **`abstract`**: 미완성된 추상 메서드나 추상 클래스를 선언할 때 사용 (자식 클래스에서 반드시 오버라이딩하여 강제로 구현해야 함).
4. **`synchronized` / `volatile`**: 멀티스레드 환경에서 여러 스레드가 동시에 자원에 접근할 때 발생하는 데이터 동기화(Thread-safe)를 처리하기 위한 한정자.

---

## 67. 생성자 (Constructor)
* **개요**: 인스턴스(객체)가 `new` 키워드를 통해 메모리에 생성될 때 맨 처음 자동으로 호출되는 특별한 초기화 메서드.
* **특징**:
    1. **리턴 타입이 아예 없음** (`void`조차 쓰지 않음). 클래스 이름과 완벽히 동일해야 함.
    2. 파라미터가 다르게 세팅된 여러 개의 생성자를 정의할 수 있음 (생성자 오버로딩).
    3. **기본 생성자 (Default Constructor)**: 파라미터가 없는 빈 생성자. 프로그램에 생성자가 하나도 없다면 컴파일러가 자동으로 만들어줌. 단, 파라미터가 있는 생성자를 하나라도 개발자가 직접 만들었다면, **기본 생성자도 명시적으로 적어주어야 파라미터 없는 객체 생성이 가능함**.

---

## 68. 자바 가상 머신(JVM)의 핵심 메모리 구조 
| 메모리 영역 | 저장되는 데이터 유형 | 특징 |
| :--- | :--- | :--- |
| **1. 메서드 영역 (Method Area)** | 클래스의 메타데이터, 바이트코드, **`static` (클래스) 변수**, 상수 풀 등. | 프로그램 시작 시 로드되며 모든 스레드가 공유함. |
| **2. 힙 영역 (Heap Area)** | `new` 키워드로 생성된 **자바 객체(인스턴스)**와 인스턴스 변수. | 가비지 컬렉터(GC)가 주기적으로 청소하는 동적 메모리 영역. |
| **3. 스택 영역 (Stack Area)** | 메서드 실행 시 전달되는 파라미터, **지역 변수(Local Variable)**. | 메서드 호출이 끝나면 즉시 메모리에서 제거됨 (LIFO 구조). |

# [Study] 자바 추상화 설계와 표준 API 클래스

## 69. 추상 메서드와 추상 클래스 (Abstract Method & Class) 
> *설계도(부모 클래스)의 뼈대만 잡고, 구체적인 구현은 자식 클래스에게 강제하는 객체지향 설계 기법*

* **추상 메서드 (Abstract Method)**: 
    * 로직이 구체적으로 구현되지 않고, 껍데기(시그니처: 리턴 타입, 이름, 파라미터)만 정의해 둔 메서드.
* **추상 클래스 (Abstract Class)의 3대 특징**:
    1. 추상 메서드를 단 하나라도 포함하고 있다면, 그 클래스는 반드시 추상 클래스(`abstract class`)로 정의해야 함.
    2. 일반 클래스와 마찬가지로 일반 멤버 변수(필드) 및 구현이 완료된 일반 메서드도 함께 포함할 수 있음.
    3. **[핵심]** 뼈대만 있기 때문에 **그 자체를 인스턴스화(객체 생성, `new`)할 수 없음**. 반드시 이 추상 클래스를 상속받는 자식 클래스를 만든 후, 부모의 모든 추상 메서드를 **오버라이딩(Overriding)**해서 내용을 채워(구현해) 넣어야만 비로소 객체 생성이 가능함.

---

## 70. 메서드 오버로딩 (Method Overloading) 
* **정의**: 하나의 클래스 내에서 **메서드 이름은 동일하게 유지하되, 파라미터(매개변수의 개수나 타입)를 다르게 하여** 여러 개의 메서드를 중복해서 정의하는 것.
* **목적**: 비슷한 기능을 수행하는 메서드들의 이름을 하나로 통일하여 가독성과 사용 편의성을 높임. (예: `System.out.println()`은 정수, 실수, 문자열 등 다양한 파라미터를 받을 수 있도록 오버로딩되어 있음).

---

## 71. 자바 필수 내장 클래스 (Java Standard API)
> *자바 개발 시 `java.lang` 및 `java.util` 패키지에서 기본적으로 제공하는 핵심 유틸리티 클래스들*

| 클래스명 | 용도 및 실무적 특징 |
| :--- | :--- |
| **`String`** | - 문자열 처리를 위한 클래스.<br>- 자바에서 문자열은 C언어 같은 기본 자료형(Primitive type)이 아니라 **클래스를 사용하는 객체(Reference) 타입**임.<br>- *특징: 한 번 생성되면 내부 값이 변하지 않는 불변(Immutable) 객체.* |
| **`StringBuffer`** | - 긴 문자열 조합(`+` 연산) 시 `String` 객체가 계속 새로 생성되어 성능이 저하되는 것을 막아주는 가변형 문자열 클래스.<br>- <br>> *아키텍트 팁: 멀티스레드 동기화가 필요 없다면, 더 가볍고 빠른 **`StringBuilder`**를 사용하는 것이 실무 표준입니다.* |
| **`Wrapper`** | - `int`, `double` 등 자바의 8가지 기본 자료형을 객체 지향적으로 다룰 수 있도록 감싸서(Wrap) 만든 클래스 (예: `Integer`, `Double`).<br>- 컬렉션 프레임워크(List, Map 등)에는 객체만 들어갈 수 있으므로 매우 자주 쓰임. |
| **`System`** | - 콘솔 입출력(`System.out`, `System.in`), 시스템 시간 읽기, 프로그램 강제 종료(`System.exit`) 등 운영체제와 관련된 유용한 기능들을 제공함. |
| **`Scanner`** | - 입력 스트림(Input Stream)으로부터 데이터를 편리하게 타입별로 입력받는 클래스.<br>- 키보드 입력뿐만 아니라, 객체 생성 시 입력 스트림을 변경하면 파일이나 네트워크 등에서도 데이터를 받아올 수 있음.<br>- *참고: 복잡한 대용량/고급 입출력은 Java I/O 패키지 클래스를 직접 쓰는 것이 좋음.* |
| **`Random`** | - 난수(Random Number)를 생성하는 클래스. <br>- 일련의 수학적 규칙에 따라 생성되므로, **현재 시간 정보 등을 시드(Seed)로 주어** 예측하기 어려운 더 유용한 난수를 만듦.<br>- *참고: 범위 지정 시 기본적으로 0부터 시작하므로, 1부터 필요할 때는 생성된 난수에 `+1`을 해줘야 함.* |
| **`StringTokenizer`** | - 특정 구분자(Delimiter: 공백, 콤마, 탭 등)로 연결된 문자열을 손쉽게 잘라내어(파싱) 분리해 주는 클래스.<br>- 특히 콤마로 구분된 CSV 데이터를 파싱할 때 유용하며, `nextToken()` 메서드로 데이터를 차례대로 가져옴.<br>> *아키텍트 팁: 최근 자바 실무에서는 정규표현식으로 배열을 바로 뱉어주는 **`String.split(",")`**을 더 선호하는 추세입니다.* |

# [Study] 자바 예외처리, 멀티스레드 및 람다식 기초

## 72. 프로그램 오류의 종류 (Error vs Exception)
* **컴파일 오류 (Compile Error)**: 
    * 컴파일러가 코드를 기계어(바이트코드)로 번역하지 못하는 문제. 
    * 보통 프로그램 소스 코드의 문법(Syntax)이 잘못되었거나 오타가 있을 때 발생함 (IDE에서 빨간 줄로 미리 알려줌).
* **런타임 오류 (Runtime Error)**: 
    * 컴파일은 정상적으로 완료되었으나, 프로그램을 **실행하는 과정(Runtime)**에서 예기치 않게 발생하는 오류. (예외처리의 주 대상)

---

## 73. 예외 (Exception)가 발생하는 대표적 상황 6가지
1. **파일 I/O**: 파일 입출력을 할 때 파일 또는 디렉터리 이름을 잘못 기재했거나 존재하지 않을 때.
2. **네트워크**: 네트워크 프로그램에서 접속할 서버의 IP/포트 주소가 틀렸거나 서버가 다운되었을 때.
3. **하드웨어**: 키보드 등에서 값을 입력받아야 하는데, 장치가 연결되지 않았을 때.
4. **배열 초과 (IndexOutOfBounds)**: 선언된 배열의 크기를 초과해서 존재하지 않는 배열 원소(Index)에 접근할 때.
5. **널 포인터 (NullPointer)**: 초기화하지 않은 객체(Null)의 메서드나 필드를 참조하여 사용할 때.
6. **데이터베이스 (SQL)**: DB 연동 프로그램에서 DB 주소, 아이디, 비밀번호, 스키마, SQL 쿼리문 등이 잘못되었을 때.

---

## 74. 예외 처리 메커니즘 (Exception Handling)
> *프로그램이 비정상적으로 뻗는(종료되는) 것을 막고, 우아하게 복구하거나 안전하게 종료하기 위한 로직*

### 74.1 예외 처리 핵심 동작 흐름
1. **`throws` 선언**: 특정 메서드 호출 시, 해당 메서드가 예외를 발생시킬 수 있음을 명시(`throws`)하고 있다면 반드시 예외 처리를 해줘야 함.
2. **`try` 블록**: 예외가 발생할 가능성이 있는 위험한 코드를 `try` 블록 안에 넣고 실행함.
    * **예외 발생 시**: 실행 흐름이 즉시 중단되고 `catch` 블록으로 점프함.
    * **예외 미발생 시**: `catch` 블록을 건너뛰고 `finally` 블록으로 이동함.
3. **`catch` 블록**: 발생한 예외 타입에 해당하는 `catch` 블록이 있는지 탐색하여, 일치하는 블록의 에러 처리 복구 코드를 실행함.
4. **`finally` 블록**: 예외 발생 여부(`catch` 실행 여부)와 상관없이 **무조건 마지막에 실행되는 블록**. 주로 열어둔 파일이나 네트워크 소켓, DB 연결 등의 자원을 안전하게 닫을(Close) 때 사용함.

---

## 75. 프로세스(Process)와 스레드(Thread)의 이해
* **프로세스**: 운영체제로부터 자원을 할당받아 **실행 중인 프로그램** 그 자체.
* **스레드**: 프로그램인 동시에 프로세스 내에서 여러 작업을 병렬로 실행할 목적으로 생성한 **실행 흐름의 단위** (경량 프로세스라고도 부름).
* **자원 공유의 이점**: 스레드는 프로세스 안에서 '스택(Stack) 영역'만 독립적으로 가지고, 나머지 시스템 자원(메서드 영역, 힙 영역)은 서로 공유함. 따라서 프로세스를 여러 개 띄우는 것보다 시스템 메모리 자원을 크게 절약할 수 있음.

### 75.1 스레드의 주요 상태 (Lifecycle)
* **Run (실행/대기)**: CPU를 할당받아 실행 중이거나 실행을 기다리는 상태.
* **Blocked (일시 정지)**: 특정 자원의 락(Lock)을 기다리거나 입출력(I/O) 지연 등으로 인해 잠시 멈춰있는 상태.
* **Dead (종료)**: `run()` 메서드의 실행을 모두 마치고 스레드가 완전히 종료된 상태.

---

## 76. 자바 스레드 구현 방식 2가지
> *아키텍트 팁: 자바는 '다중 상속'을 지원하지 않으므로, 실무에서는 무조건 2번(Runnable) 방식을 표준으로 사용합니다.*

1. **`Thread` 클래스 상속 (`extends Thread`)**:
    * 스레드 기능을 하는 클래스를 별도로 분리하여 직접 `Thread`를 상속받는 방식. 다른 부모 클래스를 상속받을 수 없다는 치명적 단점이 있음.
2. **`Runnable` 인터페이스 구현 (`implements Runnable`)**:
    * 스레드로 동작해야 하는 클래스가 이미 다른 클래스를 상속받고 있을 때 사용. 
    * `Thread` 클래스를 상속할 때처럼 내부에 **`run()` 메서드만 오버라이딩(Overriding)**하여 구현하면 되며, 스레드의 실행 구조상 차이는 없음. (유연성이 매우 높음).

---

## 77. 멀티 스레드와 동기화 (Synchronization)
* **멀티 스레드 프로그래밍**: 하나의 거대한 작업(Task) 수행을 목적으로 여러 개의 스레드를 동시에 사용하여 처리 속도를 높이는 프로그래밍 기법.
* **스레드 동기화**: 여러 스레드가 동시에 같은 자원(공유 변수 등)에 접근할 때 발생하는 데이터 오염 문제를 해결(상호 배제)하는 기법 (`synchronized` 키워드 등 사용).

---

## 78. 람다식 (Lambda Expressions)과 함수형 언어
* **함수형 프로그래밍**: 함수에 기반을 두고 데이터를 중심으로 프로그램을 기술하는 패러다임. (대표 언어: LISP, Haskell, Erlang, F#, Clojure 등).
* **람다식의 정의**: 자바 8(Java 8)에 도입된 가장 큰 특징으로, 입력과 출력을 가능하게 하는 단순화된 구조의 **익명화된 함수 표기법**(이름 없는 함수). 함수형 프로그래밍을 자바에서 구현하기 위한 핵심 문법임.

---

## 79. 함수형 인터페이스 (Functional Interface)
* **정의**: 내부에 추상 메서드가 **단 1개만** 존재하는 인터페이스. 
* **특징**: 자바에서 람다식을 변수에 저장하거나 매개변수로 전달하려면, 반드시 그 람다식이 이 '함수형 인터페이스'를 기반으로 작성되어야 함. *(실무에서는 컴파일 에러를 막기 위해 상단에 `@FunctionalInterface` 어노테이션을 붙임).*
