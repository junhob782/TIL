메모리 구조
스택(Stack): 함수 호출 구조와 동일하게 작동 (지역변수, 매개변수). LIFO 구조.
힙(Heap): 사용자가 직접 관리하는 동적 할당 영역.

주소(Address)의 이해
정의: 변수가 할당된 메모리의 시작 주소.

할당 단위: 32bit/64bit 시스템 상관없이 1칸당 1byte씩 할당됨.
크기: int형은 4byte(4칸)를 차지하지만, 주소값 자체의 크기는 포인터 크기(시스템 비트 수)에 따라 고정됨.
핵심: 가리키는 데이터가 1byte(char)든 8byte(double)든, 주소값(포인터)의 크기는 동일하다.

엔디안 (Endianness)
리틀 엔디안 (Little Endian): 낮은 자릿수(LSB)부터 메모리에 저장 (인텔 계열).
빅 엔디안 (Big Endian): 높은 자릿수(MSB)부터 메모리에 저장 (네트워크 전송 등).

4. C언어 핵심: 포인터와 입출력
포인터 문법 (필수 암기)
pa = &a: 주소 구하기 (변수 a의 위치를 pa에 저장)
*pa = 10: 값 접근하기 (주소 pa로 가서 10을 대입)
*pa: 주소에 저장된 실제 값(Value).

함수와 스코프
재귀 함수: 자기 자신을 호출하는 함수 (스택 메모리 사용).
변수 접근: 가까운 블록(함수)의 변수부터 우선 접근한다 (Local scope 우선).

입출력 함수 (scanf)
값을 입력받을 변수의 **주소(&)**를 넘겨줘야 한다.
이미 주소인 경우(배열 이름, 포인터 등)에는 & 연산자가 필요 없다.

서식 지정자 및 특수문자 (수정본)
\n: 개행 문자 (줄바꿈)
%d: 10진수 정수 (Decimal) (※ 메모의 문자/숫자 반대 수정)
%c: 문자 한 개 (Char)
%f: 실수 (Float)
%s: 문자열 (String)

5. 자료구조와 알고리즘
 (배열)다차원 배열의 순회
메모리 효율을 위해 바깥쪽부터 순회
(행) $\rightarrow$ 안쪽(열) 순서로 도는 것이 정석.
예: int score[3][4]인 경우, [3](행)이 천천히 변하고 [4](열)가 빨리 변하도록 for문을 구성.
영상/그래픽 좌표계수학좌표 $(x, y)$와 달리 프로그래밍(행렬)에서는 y(행/세로)를 먼저 쓰고,
x(열/가로)를 뒤에 쓰는 경우가 많음. (arr[y][x])

# 📚 C Programming Study Notes (2026-02-09)

## 1. 포인터 (Pointer)
메모리의 주소를 다루는 핵심 개념

### 포인터의 종류
| 구분 | 설명 | 비유 |
| :--- | :--- | :--- |
| **변수** | 실제 값을 저장 | 우리 집 (실체) |
| **단일 포인터** | 실제 값이 있는 **위치(주소)**를 가리킴 | 우리 집 주소 |
| **이중 포인터** | 포인터 변수의 **주소를 저장하는 포인터** | '우리 집 주소'가 적힌 종이의 위치 |

---

## 2. 메모리 동적 할당 (Dynamic Memory Allocation)
프로그램 실행 중에 필요한 만큼 메모리를 **힙(Heap)** 영역에서 빌려 쓰는 기법

### 💡 핵심 원칙
1. **짝 맞추기**: `malloc`과 `free`의 횟수는 반드시 일치해야 함.
2. **해제 우선**: 할당 코드를 짰다면, **해제(`free`) 코드부터 작성**하고 내용을 채우는 습관 들이기.
3. **해제 방법**: `malloc`으로 할당한 공간은 오직 `free`로만 반납 가능.

### 주요 함수 정리
* **`malloc(size)`**: 메모리 공간 할당 후 `void *` 반환 (초기화 X, 쓰레기값 들어있음)
* **`calloc(n, size)`**: 할당된 공간을 **0으로 자동 초기화**
* **`realloc(ptr, size)`**: 이미 할당된 공간의 크기를 **조절(확장/축소)**
* **`free(ptr)`**: 다 쓴 메모리 공간을 시스템에 **반납**

---

## 3. 사용자 정의 자료형 (User Defined Type)

### 구조체 (Struct)
서로 다른 자료형을 하나로 묶어 관리하는 자료형

```c
struct student {
    int num;      // 4 bytes
    double grade; // 8 bytes
}; 
// 총 크기: 12 bytes (패딩 비트 제외 시)

### 파일 열기, 쓰기
FILE *fp = fopen("a.txt", "r"); // 파일 열기
// ... 작업 수행 ...
fclose(fp); // 파일 닫기 (필수!)

함수명,설명
fseek,파일 포인터의 위치를 이동 (주로 임의 접근 시 사용)
fgets,문자열을 한 줄씩 읽음 (파일 끝 도달 시 NULL 반환)
fscanf / fprintf,"scanf, printf와 사용법 동일 (대상만 파일)"
fflush,버퍼에 남은 데이터를 장치(파일)로 즉시 기록
fread / fwrite,데이터의 크기와 개수를 지정하여 입출력 (바이너리에 주로 사용)

# 📂 C Programming Study Notes (2026-02-09) - Part 2

## 1. 파일 입출력 (File I/O)

### 파일 열기 (fopen)
```c
FILE *fp = fopen("a.txt", "r");

모드,설명,비고
r,Read (읽기),파일이 없으면 에러 발생
w,Write (쓰기),파일이 있으면 내용 삭제 후 새로 생성
x,eXecute,(참고: C11 표준에서는 'wx' 형태로 배타적 생성 모드로 사용됨)

함수명,기능 설명
fseek,파일 포인터의 위치 이동 (+ 모드에서 읽기/쓰기 전환 시 필수)
fgets,문자열을 한 줄씩 입력받음 (데이터를 모두 읽으면 NULL 반환)
fscanf / fprintf,"기존 scanf, printf와 사용법 동일 (대상만 파일로 변경)"
fflush,출력 스트림 버퍼의 내용을 즉시 장치(파일)로 기록
fread / fwrite,데이터의 크기와 개수를 지정하여 입출력 (바이너리에 주로 사용)

매크로명,설명
__DATE__,컴파일을 시작한 날짜
__TIME__,컴파일을 시작한 시간
__FILE__,전체 디렉토리 경로를 포함한 소스 파일명
__FUNCTION__,현재 실행 중인(매크로가 사용된) 함수 이름
__LINE__,매크로가 사용된 소스 코드의 행 번호


